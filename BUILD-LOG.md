# 我是怎么搭这个项目的

> 这份文档记录我搭这个 OnCall Agent 时的**顺序和理由**，不是使用说明——怎么跑起来看 [README](./README.md)。
>
> 我用的是 OpenSpec 的方式：每加一个能力，先把"为什么要做、要改什么、**不做什么**"写成一份变更提案，
> 再实现、验证、最后归档。整个项目一共 35 个归档变更，分成六个阶段。这份文档按阶段讲我当时怎么想的。
>
> 内容比较长。**如果时间有限，可以直接跳到最后一节「回头看：六个阶段」**，那里是整体结论。

## 整体概览

| 阶段 | 变更数 | 在解决什么 |
|---|---|---|
| **A 地基** | 7 | 骨架、契约、仓储、认证、隔离、模型边界、本地设施 |
| B 知识库 | 6 | 数据进得来、搜得到 |
| C 聊天 | 5 | 对话能用，模型能自己调工具 |
| D AIOps | 7 | 能干活：从告警到带证据的诊断报告 |
| E 交付与观测 | 3 | 就绪判断、可观测、演示数据、文档站 |
| F 后续修复与重构 | 7 | 真机跑出来之后暴露的问题，回头改 |

**A 阶段我一件"能看见"的功能都没做。** 因为后面每个功能都会用到"目录怎么放、接口什么形状、
当前用户是谁、数据归谁、模型怎么调、数据库在哪"。这些不先定死，后面每加一个功能就要回头改一次，
等于把同一件事做二十遍。

## A 阶段：地基（7 个变更）

### 1. 立骨架

**做了什么**：定下 monorepo 目录和技术栈；做出最小可运行骨架（后端一个 `/health`、前端一个能打开的页面）；
配置只从两份本机 JSON 读、浏览器只拿白名单；`.gitignore` 从一开始就忽略真实配置和凭据；
把目录边界、导入规则、敏感文件不被追踪这些"人的约定"写成跑得起来的测试。

**为什么这么做**：我最怕的是后面每个功能各写一套。技术栈、边界、契约入口、最小门禁这四样是后面天天要用的东西，
不先定死就要反复返工。

**留下什么**：`monorepo-foundation` 规格。今天回头看，最值钱的是三条约束——导入不产生副作用、
配置只认本地 JSON、前端配置走白名单。

**我的思考**：这一步对我来说就是四件事——**技术栈定义、边界定义、契约定义、最小门禁测试定义**。
其中我特意关注了两点：一是导入包的问题，后面会有很多函数和类分散在不同目录，路径一乱就容易出问题；
二是前端那道保险，保证后端代码不管怎么冗余都影响不到前端。

### 2. 契约先行

**做了什么**：把共享契约建成前后端的唯一事实来源——统一成功/失败响应格式、稳定错误目录、
接口路径、流式事件类型，以及一份机器可读的清单；后端统一封装响应和请求 ID；前端做 typed 客户端；
再加跨语言一致性测试，禁止任何人自己造临时字段。

**为什么这么做**：契约最贵。它一变前端后端都要改，越晚定改得越多；而且后面几乎所有功能都会走 HTTP 或 SSE，
它是所有功能的共同底座。

**留下什么**：一个契约能力，后面 35 个变更里有 21 个都动过它——也就是说后面三分之二的开发都在往同一份契约上加东西，
而不是各写一套。所有失败都走同一个格式，前端只认一个字段，不用猜这次返回的是 `error` 还是 `detail`。

**我的思考**：这一步就是定义不同语言之间的契约怎么对齐，以及错误怎么统一表达。
**关键点是它靠前后端两份语言各自维护、并行检查来保证对齐**，而不是靠自觉。

### 3. 仓储层

**做了什么**：建立统一的数据库接入层（引擎、会话、事务、迁移），并定下"业务代码不直接碰数据库"的规矩；
这一步故意不实现任何业务表的增删改查。

**为什么这么做**：如果每个领域自己写 SQL、自己管连接，就会测不动、换不动，
而且**导入模块的时候可能顺手把数据库连接建了**。

**留下什么**：一个所有后续领域都复用的持久化边界。

**我的思考**：这一步我理解成先把"怎么存取数据"收口，业务代码要数据就找仓储层要，不自己动手。

### 4. 登录认证

**做了什么**：用户表和会话表 + 迁移；密码存 Argon2 哈希；登录发高熵 token，**数据库只存 token 的哈希**；
注册、登录、登出、当前用户四个接口全部复用第二步的契约；前端只把 token 存在本地。

**为什么这么做**：我的判断是**认证不是一个功能，而是后面所有功能的身份来源**，
所以它必须复用已有的契约和仓储层，不能自己另造一套。反过来做（先做聊天、之后再补认证）的话，
认证只能在中间到处打补丁。

**留下什么**：一个认证能力，加上"当前用户"这个依赖——后面每个接口的身份都从它来。

**我的思考**：这一步定义的是邮箱和密码从前端到后端的格式，以及后端怎么处理它们（哈希、查重、认证），
还有数据库上的唯一索引。另外我把它理解成**后面所有功能的前提**，包括租户隔离也是建立在它上面的。

### 5. 租户隔离

**做了什么**：把"当前用户 → 数据归属"固化下来；规定受保护仓储的方法第一个参数必须是 owner；
过滤条件必须写进数据库语句里，不能"查完再筛"；越权统一返回"找不到"；
另外写了一个扫描脚本自动检查所有仓储的方法签名，并挂在测试里。

**为什么这么做**：先按 ID 查、再补一句"这是不是我的"，这种写法每个接口都要记得补一次，漏一个就是漏洞；
而且"别人的资源"和"不存在的资源"如果返回不同错误，别人还能靠报错差异枚举出哪些 ID 真实存在。

**留下什么**：一条全局隔离规则，写进项目指南，聊天、知识库、向量、MCP、AIOps、案例、反馈全部要遵守；
扫描脚本成为长期门禁，后面每加一个仓储都自动检查。

**我的思考**：这一步定义的是一个**全局的隔离机制**——后面不管哪个流程，都按当前用户来隔离。
而且隔离必须一开始就定好，如果后面每个模块自己加条件，就会有漏掉的风险。**我的目标不是"我加了权限检查"，
而是"权限检查漏不掉"。**

### 6. 模型调用的统一边界

**做了什么**：模型配置用 typed 校验、只从本机 JSON 读；给对话、向量化、重排三类能力建统一入口并固化参数；
**延迟创建客户端**（导入模块时什么都不做）；**错误脱敏**（把 key 替换掉再往上抛）；
加 readiness 探测（真的发一次最小请求）；不引入厂商 SDK。

**为什么这么做**：我担心四件事——每个功能自己设超时重试导致参数漂移、从环境变量偷读 key、
**导入模块就联网**（测试和脚本都可能触发真实调用）、以及第三方报错把请求头连 key 一起带出来。

**留下什么**：一个模型能力，加上一个很实际的好处——**以后换模型只改配置，不改代码**。
后来我真的换了对话模型的厂商，改动只落在三个文件里，业务代码一行没动。

**我的思考**：这一步定义的就是模型调用的边界：key 在哪里读、导入怎么测、调用前怎么确认可用。
具体做法有延迟创建、key 脱敏、readiness 三种。本质上它就是**把"怎么调模型"收在一处**，
所以以后要改模型，我知道该去哪里改。

### 7. 本地基础设施与向量库

**做了什么**：一份 compose 只跑五个基础服务（etcd、MinIO、Milvus、Attu、Alertmanager），版本固定、带健康检查；
明确禁止把后端、前端、MCP 塞回容器；删掉配置里重复的镜像版本，让 compose 成为版本号的唯一来源；
另外做了向量库适配器（延迟连接、固定维度和索引参数、按归属过滤读写）。

**为什么这么做**：前后端不进容器是我**主动选的**，不是没想到——好处是改一行代码立刻生效、不用重建镜像、
调试直接，而且本机凭据不会被烤进镜像；代价是别人要自己装 Node、Python 和 Docker。
另外"版本号只写一处"是刻意的：同一个事实写在两个地方，早晚会打架。

**留下什么**：五个服务、固定端口，以及一个按归属读写的向量库适配器，后面整个知识库都建立在它上面。

**我的思考**：这一步定义的是向量数据库和环境的要求。前后端不进 Docker 是因为改动起来方便；
版本号和端口都定义在一处，是为了以后改动容易、出错也好纠正。

### A 阶段结束时，我手里有什么

一个**还看不到业务功能、但已经能跑起来**的系统：定好的目录和技术栈、一份前后端共用的契约、
一个能注册登录的身份系统、一条"数据只能归自己"的硬规则（还带自动检查）、
一个统一的模型调用入口、五个基础容器和向量库适配器。

**后面每个功能都是往这套地基上加东西，而不是回头改地基。**

## B 阶段：知识库

B 阶段做的是整个项目里第一个"能用眼睛看到"的功能：知识库。
六步的顺序还是老规律——**先底座（后台任务、前端外壳），再做数据（文档、切片、索引），
然后做能力（检索），最后才做界面**。

### 1. 持久后台任务

**做了什么**：两张表（任务 + 事件）+ 迁移；一套 worker 运行时（原子领取、租约续租、超时、退避重试、取消）；
事件的单调序号与按序号重放；四个受保护的管理接口。

**为什么这么做**：文档索引、诊断这类长耗时操作**不能依赖 HTTP 连接或进程内临时任务存活**。
临时任务缺四样东西——没有持久化（重启就丢）、没有重试和超时、没有取消语义、没有可观测。

**留下什么**：一套所有长任务复用的底座；SSE 事件断线重连的实现基础；
一条反复被引用的原则——**长耗时操作一律走持久任务**。

**我的思考**：这一步保证的是长时间请求不会失效，并且提供中断后恢复的机制。
用进程内实现相对不太好，所以要做一套小而完整的任务系统：持久化到 SQLite、断点恢复、
worker 心跳续租、过期后由别的 worker 接管。领取任务靠的是**带条件的原子更新**，
不是靠 id 唯一——条件写在 `WHERE` 里，数据库保证只有一个 worker 改得动。

### 2. 前端工作台外壳

**做了什么**：路由分成公共认证页和受保护工作台两类；首次导航只恢复一次认证；
未登录跳登录页并记住原本要去哪；受保护状态统一登记清理；本地只存 token；
四个一级页面先放占位页，明确写"待实现"。

**为什么这么做**：后面每个页面都要用到同一套认证接入、状态清理和布局。
如果等做某个页面时顺手搭，每个页面都会各写一套。所以先把**公共规矩**定下来。

**留下什么**：一个前端能力规格；后面所有页面都长在这个壳里，没人再动布局和认证接入；
401 → 统一清理受保护状态；认证恢复只做一次。

**我的思考**：界面这一块是为了统一一个标准——后面加功能不用每次自己重写前端，
而是对应着往里填内容就好。另外这里第一次把 401 的处理收口到一处：
拿到 401 就删掉本地 token、清空身份、状态回到未登录，并通知所有存着用户数据的 store 一起清干净。

### 3. 文档上传与切片

**做了什么**：每个用户一个隐式默认知识库；上传政策由后端权威执行（10 MiB、Markdown/PDF、
扩展名和 MIME 都校验、PDF 提取文本）；文档记录里存 SHA-256、正文、软删除状态和**实际使用的切片参数**；
三个切分策略但只有一个入口；预览有界（最多 12 段）。

**为什么这么做**：需要"安全、可追溯且可重复切分"的文档来源。尤其是一条——
**预览和索引必须来自同一个切分入口**，否则用户会看到"预览里有、但搜不到"的假象。

**留下什么**：索引那一步的直接输入；契约第五次扩展；
三个"可追溯"要素——内容哈希、实际切参、软删除留痕。

**我的思考**：如果预览和索引不一个入口，可能出现切片方式对不上的情况。
软删除是为了让旧版本留着做溯源，而"活跃版本只能有一份"靠的是**部分唯一索引**——
只对未删除的行生效，两者配合才行。

### 4. 文档索引

**做了什么**：把索引做成独立任务（有自己的状态机和记录），由客户端显式创建；
注册成持久任务的一个 handler；固定执行顺序：复用同一切片入口 → 分批算向量 → 校验数量 →
先删该文档的旧向量 → 一次批量插入 → 再校验数量 → 落状态。

**为什么这么做**：切分、算向量、写向量库都慢，而且**跨两个持久系统**（SQLite 存文档、Milvus 存向量），
任何一步都可能失败。放在上传接口里既会超时，也无法恢复。

**留下什么**：第一个真正跨系统的长任务范例（后面诊断和案例提升都照这个模式做）；
Milvus 里带完整归属字段的向量数据。

**我的思考**：这两个动作都耗时，所以要用持久化任务来做；拆开上传和索引是为了体验更好，
不会因为上传久就一直等着。删除旧向量是因为最终只能保证"一份有效数据版本"留在库里——
它同时兜住了"过期的块"和"上次写了一半的块"两种情况。

### 5. 混合检索与重排

**做了什么**：一个只给模型调用的检索工具；两路召回并行（向量 + 关键词）；
定制 tokenizer（中文单字/双字 + ASCII 运维词）；RRF 融合；真实重排；
候选数量层层收敛（每路 20 → 融合 20 → 重排 → 最多返回 5 条）；不设最低分阈值。

**为什么这么做**：**一次性做完整管线，避免过渡方案**——过渡方案一旦上线就会有下游依赖，
之后每改一次都要动一片。另外两路信号性质不同（便宜的近似信号 vs 昂贵的精确信号），
所以顺序只能是"先粗召回缩小范围，再用模型精排"。

**留下什么**：聊天链路的知识来源；每条结果带完整的分阶段证据（两路排名、RRF 分数、重排分数），
所以界面上能说清"这条是怎么被找出来的"。

**我的思考**：先召回再重排是出于成本——直接重排太贵；而且两路分数不是一个量级，
没办法直接对比，所以要用 RRF 来融合。另外召回之后要回 SQLite 换权威文本，
**向量库负责找，SQLite 负责说这是真的**。

### 6. 知识库页面

**做了什么**：把前面五步的能力串成一个闭环——上传、文档列表、行内详情、切片预览、
索引跟踪（约每 2 秒轮询、刷新后恢复未完成任务）、失败重试、手动重建、冲突确认覆盖、删除。

**为什么这么做**：界面上的每个动作都对应一个真实接口，**服务器是唯一事实来源**。
不用 localStorage、不用静态数组模拟数据、不在前端复制后端的状态机。

**留下什么**：一个可复制的前端范式（typed client + store + 服务器为唯一事实来源 + 状态如实显示），
后面的聊天和诊断工作区都照这个模式做。

**我的思考**：上传和索引在界面上是**两个动作、两个状态**——上传成功不等于能搜到，
界面要如实分开显示。前端只做三件事：调接口、显示真实状态、破坏性操作要用户确认。

### B 阶段结束时，我手里有什么

一条走通的链路：**上传文档 → 切片 → 入向量库 → 双路召回 + 融合 + 精排 → 界面上能搜到**。

还有两个可以复用的东西：一套持久任务底座（后面诊断要用）和一个前端范式（后面两个工作区要用）。

## C 阶段：聊天

C 阶段做聊天。顺序是：**先定"历史存在哪"，再定"记忆怎么整理"和"个性化怎么来"，
然后做核心的流式问答 + 自主调工具 + 审计，最后才做界面**。

### 1. 会话管理

**做了什么**：两张表（会话 + 消息）；消息带"会话内自增序号"并有唯一约束；
追加消息在一个事务里做三件事（算序号、插消息、更新会话时间）；首条用户消息生成有界标题；
清空消息与删除会话语义分开；前端只存 token 和"当前会话 id"。

**为什么这么做**：如果先做"流式回答"，那"历史存哪"会被临时解决掉——存在前端内存里、
或者每次请求把整段历史来回传，以后再想搬到服务端就要动整条链路。所以先把消息存哪儿定死。

**留下什么**：流式回答的持久化目标；一套前后端都遵守的边界——聊天数据只在服务端，浏览器只存 token。

**我的思考**：顺序不用时间戳，用数据库里的序号，因为同一毫秒可能产生两条消息、不同机器时钟还可能不一致，
顺序是业务事实，就该让数据库保证；追加消息必须在一个事务里，否则会出现"消息存了、会话时间没更新"的半成品。

### 2. 会话记忆模式

**做了什么**：每个会话独立的记忆状态（模式、摘要、已压缩到第几条、token 用量）；
三种模式——每 30 个完整轮次、上下文 70%、手动；外加一条所有模式通用的硬闸门（95% 熔断）；
而且这个判断发生在消息落库之前。

**为什么这么做**：之前为了不超模型上限，做法是把最旧的消息**丢掉**——有损、不可选、
而且到接近上限才不稳定地失败。这一步换成"**摘要 + 保留完整历史**"：压缩只更新派生摘要，原文一条不删。

**留下什么**：模型上下文的新组成（摘要 + 未压缩消息 + Prompt/Skill 目录）；
阈值逻辑必须"可测"这条要求（专门覆盖 69/70/95 三条边界）。

**我的思考**：压缩是对上下文做一份摘要；如果直接删历史，后面就什么都没有了。
30 轮和 70% 是两个自动整理的触发条件，95% 是全模式通用的熔断闸门——
它发生在消息落库之前，所以不会留下"消息存了但模型没回答"的半截状态。

### 3. 提示词与渐进式 Skill

**做了什么**：owner-scoped 的 Prompt（单选）和 Skill（多选）资产；Skill 是有标准的文件
（必须叫 SKILL.md、UTF-8、≤256KB、有 frontmatter、名字规范化、同用户唯一）；
每轮把 system prompt 拼成三段（平台安全规则 → 用户 Prompt → Skill 摘要目录）；
再加一个按需读正文的 `load_skill` 工具。

**为什么这么做**：Skill 正文全塞进上下文会撑爆窗口、推高成本、稀释注意力。
所以用**渐进式披露**：先给目录，正文按需再读——和人先看目录再翻章节一样。

**留下什么**：Agent 有了可控的个性化能力；"先给摘要目录、正文按需取"这个结构后面在 AIOps 里又用了一次。

**我的思考**：Skill 是一种渐进式披露的加载机制。装配顺序固定三段，
**用户的 Prompt 只是其中一段内容，覆盖不了平台的安全规则**；另外按需加载必须绑定"本轮已选 + 归属"，
否则它自己就成了越权入口。

### 4. 流式问答与工具审计

**做了什么**：用 LangChain 的 `create_agent` 建聊天运行时，模型在内置工具之间自主选择（禁止每轮固定先检索）；
owner-scoped 的流式接口输出六类事件（正文增量、真实 reasoning、工具生命周期、引用、完成、错误）；
消息按完整回答边界持久化（开始前存 user，成功后才存 assistant）；新增通用工具调用审计表和查询接口。

**为什么这么做**：**让模型自主选工具，流程就变得不确定；不确定的过程必须能查**。
所以流式和审计是一起做的——一个让用户看到过程，一个让事后能追溯。

**留下什么**：聊天闭环（自主调工具 → 实时流式 → 落库 → 可审计）；
审计变成通用能力，后面 AIOps 诊断的工具调用复用同一套；SSE 公共字段增加了稳定 `sequence`。

**我的思考**：把模型自主调工具这件事拆开看，有三个机制各管一件事——
**SSE 事件**解决"过程中能看见"，**消息落库**解决"刷新后还在"，**工具审计**解决"事后能查到调了什么"。
审计只记参数键名和耗时，不记取值，所以既能排查又不泄露内容。

### 5. 聊天工作区与引用

**做了什么**：把前面五步的能力摆成工作台——左侧会话列表、中间消息流 + 输入框 + 记忆控件、
右侧 Prompt/Skill 面板、每条回答下面是引用卡片（最多 5 条，能展开看完整检索轨迹）；
正文用 marked + DOMPurify 安全渲染；reasoning 和工具活动默认折叠。

**为什么这么做**：界面最后做——因为它是所有能力的入口，前面没定好，界面就要改。
这一步唯一新增的判断有两条：**引用在"实时流"和"历史恢复"两条路径上必须带同样的证据**，
以及**模型输出属于不可信内容，渲染前必须消毒**。

**留下什么**：C 阶段闭环；一个可以照抄的工作区范式（左列表、中主区、右配置，状态如实显示）——
D 阶段的诊断工作台就是照这个做的。

**我的思考**：界面上没有发明任何业务规则，用的全是已有接口；引用卡片展示的是证据不是分数，
用户能看出这条引用是怎么被找出来的。

## D 阶段：AIOps

D 阶段做整个项目的重头戏：从告警到带证据的诊断报告。
顺序仍然是那套——**先输入，再工具，然后编排与证据，再沉淀，最后界面**，
最后两步是回头补治理。

### 1. 告警与日志输入

**做了什么**：可配置的多来源告警读取（并发、标准化成同一套模型、单源失败降级、全失败返回 503）；
一个只读的活跃告警接口；一个独立的日志上传 CLI，必须人工执行并显式声明目标；明确不生成任何结论。

**为什么这么做**：诊断需要真实输入，但输入层必须**只负责输入**。另外上传日志会真实写进用户的云账号，
所以它不能是应用的一部分，也不能被测试或启动顺手触发。

**留下什么**：诊断的输入模型；演示数据的上传通道（后面 10 套 fixtures 就是走它上来的）。

**我的思考**：输入层去下结论明显不合适。它的价值不只是"分层好看"，而是三个具体好处——
能独立测试（喂假 payload 就能测标准化）、能独立替换（换告警系统只改 provider）、结论能回溯到当时的输入。
上传日志要显式确认，确认的是**写到哪个目标**，因为这个操作不可逆、而且不能被自动流程触发。

### 2. 真实 MCP 连接与工具

**做了什么**：owner-scoped 的连接管理（增删改查 + 启停 + **真实检查**）；URL 校验拒绝 userinfo；
超时和重试的范围用数据库约束卡死；按用户聚合工具，**同名冲突直接装配失败**；
把真实工具注入聊天 Agent 并复用统一审计。

**为什么这么做**：模型能用的工具不该只有内置那几个，应该能接用户自己的服务器。
但"接进来"必须是有界的：慢服务器不能拖死对话，冲突的工具不能靠猜，凭据不能进 URL。

**留下什么**：可扩展的工具池（后面 AIOps 直接用）；一条真实可演示的通道（官方 CLS MCP 就是这样接进来的）。

**我的思考**：两个同名工具会让用户不知道到底调的是哪个，而且系统自己也无法确定该用哪个——
所以宁可装配失败、让人改名或禁用，也不要随便挑一个。
URL 不许带凭据，是因为完整 URL 会被持久化、还会返回给前端展示，带凭据就是泄露。

### 3. 诊断编排与证据链

**做了什么**：有界的 LangGraph 图（Planner → Executor → Replanner → Report）；六张表把任务、步骤、
**规范化证据**、报告、报告与证据的关联、以及 checkpoint 持久化；五个接口；
取消和重试复用后台任务接口；模型失败时给诚实的 fallback 报告。

**为什么这么做**：要把"客户断开或进程重启后仍能持续执行"和"任何结论都能回到真实证据"这两件事做成结构，
而不是靠提示词。所以每一步的产出都落成证据记录，报告和证据之间建关联表。

**留下什么**：AIOps 的骨架——后面所有 AIOps 改动（三节点重构、证据判据、执行总账）都在这套表和图之上。

**我的思考**：把"不要编造"写进提示词，只是在请求模型自觉；靠数据结构保证才稳定。
提示词提高一次做对的概率，代码和数据结构保证下限；而且校验失败之后**不能放行**，
要降级成一份诚实的报告，明确写证据不足。

### 4. 诊断案例沉淀

**做了什么**：结构化案例表 + 来源表；自动路径（报告完成后触发，靠唯一约束幂等）和手动路径
（只存文档、不建案例）分开；案例生成文档进入知识库，参与后续检索。

**为什么这么做**：让历史故障进入**知识循环**——诊断的结论写回知识库，下次同类事故检索时能命中，
排查更快。

**留下什么**：知识循环闭环；案例库与"提升为知识案例"这条产品路径。

**我的思考**：循环就是把成功的诊断案例重新入库当知识，让后续同类事故能快速定位。
一开始是"生成报告就沉淀"，后来发现不行，改成了要有**可信报告 + 人工认可**。
幂等是为了防止同一份案例重复入库——重复会占满检索名额，把真正相关的结果挤出去。

### 5. 诊断工作台

**做了什么**：三栏控制台——左边告警与历史、中间报告与执行过程、右边证据与案例；
支持告警预填或纯文字创建、历史切换、取消、断流后按 REST 对账；报告用安全渲染；工具活动默认折叠。

**为什么这么做**：能力都做完了，最后才做界面；而且**布局直接复用聊天工作台那套骨架**，
换业务内容、不换结构。

**留下什么**：D 阶段在界面上闭环；这套三栏结构后来也承载了后面所有的诊断界面调整。

**我的思考**：界面上不发明任何状态——取消复用后台任务接口，`phase`（规划/执行）和任务状态分开显示；
SSE 断开时明确提示并给手动重订阅按钮，而不是悄悄自动重连，否则用户会以为一直是实时的。

### 6. 用户反馈

**做了什么**：四类固定反馈目标（助手消息、引用、诊断步骤、诊断报告）；服务端按**真实父记录**验证归属；
查询 / 写入 / 删除三个接口；评分与问题类型是固定目录；反馈正文不进运行日志；
前端失败时保留草稿、不假装保存成功。

**为什么这么做**：反馈是"挂在别的东西上的"，所以授权不能信客户端——必须服务端读得到那条父记录、
而且它属于你。这是唯一一个由人直接给系统下判断的入口。

**留下什么**：人的判断有了持久载体；下一步正好把它接进案例提升的门槛。

### 7. 证据与案例治理加固

**做了什么**：双层白名单（动态发现的工具 ∩ AIOps 能力白名单）；能力依赖从"固定三件套"改成按条件；
所有允许的工具都要校验输入，产物要有显式 adapter；有界的分类重试；
报告分成"可信 / 证据不足 / 执行失败"三种；案例沉淀改成人工认可后的**显式提升**，并加跨任务去重。

**为什么这么做**：跑起来之后暴露了三个断层——**工具能看见不等于能用好**、
**调用了不等于输出能当证据**、**沉淀了不等于该沉淀**。

**留下什么**：这一版是后面七个修复的起点；而且立了一条一直沿用的原则——
**"能看见 / 能调用 / 能验证 / 能作证"是四道不同的门槛**。

**我的思考**：最值钱的一条是 adapter——工具输出默认只是文本，只有显式适配过才能当证据，
于是"能调用"和"能作依据"被分开了。案例门槛则变成系统加人一起判断：
系统看证据是否完整、是否确定，人判断这条经验是否有用。

### D 阶段结束时，我手里有什么

一条完整的链路：**真实告警 → 自主工具诊断 → 规范化证据 → 带可信状态的报告 → 人工认可后沉淀成案例 →
下次同类事故能被检索命中**。

另外还有两条可以复用的东西：一套通用审计（工具调用全记录）和一个前端三栏范式。

## E 阶段：交付与观测

阶段 E 是平台搭建的收尾，三个变更做的都是"让这个项目能被别人跑起来、
能被验证、能被查阅"的事情。

### 1. 运行时就绪、可观测与运维

- 增加进程存活（`/health`）、依赖就绪（`/ready`）、配置检查（`/config/check`）、
  MCP 聚焦检查（`/health/mcp`）和本进程轻量指标（`/metrics`）五个探针；
  依赖失败时逐项返回安全结果，整体状态为 503。
- 所有 HTTP 请求生成或透传请求 ID；记录**不含请求内容和凭据**的结构化完成日志；
  统一做递归脱敏，并规范运行时生命周期日志字段。
- 提供 macOS / Linux / Git Bash 与 Windows 两个本地启动脚本：
  依次启动五个基础设施容器、数据库迁移、官方 CLS MCP、FastAPI 和 Vite；
  运行日志写入被忽略的 `apps/backend/var`。
- 更新中文 README、各平台安装指南、运维监控说明和"真实日志与告警"教程，
  明确"普通启动"与"会产生外部副作用的 fixtures"之间的界线。
- 清理应用 Dockerfile、`project.compose.json`、`create_compose_app` 及其死引用，
  把"应用只在主机运行"这条边界固定下来。
- 建立**在没有真实凭据的机器上也能跑的最终自动门禁**，
  并把需要真实模型 / Milvus / CLS MCP / CLS / 告警目标的人工链路单独记录（没跑就不说通过）。

### 2. 关联式电商演示 fixtures

- 新增十套固定且互不重复的 Java 电商故障 fixture，用统一的关联键把
  **CLS 日志、Alertmanager 告警和 Markdown SOP** 串起来。
- 扩展现有的日志上传脚本，增加 `java-ecommerce` 这个固定十场景 profile，
  同时保持原有量化 profile 与 count 参数兼容。
- 新增显式的 Alertmanager 发布脚本：只向用户明确指定并确认过目标发布十条告警。
- 新增显式的 SOP 导入脚本：通过真实认证接口上传十份 Markdown、创建持久索引任务，
  并在有界时间内等待结果。
- 新增数据与生成边界的自动化测试，验证数量、唯一性、跨载荷关联、内容安全、
  失败时退出码正确、以及导入无副作用。
- 补充人工真实 smoke 的执行顺序与诚实记录要求；**普通启动、测试和门禁都不会产生外部写入**。

### 3. OpenSpec 文档站

- 用 VitePress 发布全部主规格、进行中的变更和归档变更，
  通过 `<!--@include: ...-->` 引用原始 artifacts，**不复制正文**（避免出现第二份事实来源）。
- 建立 `docs/openspec` 到仓库 `openspec` 的相对符号链接，
  并为 Windows 的符号链接能力提供检查与故障提示。
- 为变更页面建立统一 frontmatter、中文导航、索引和侧边栏，覆盖提案、设计、任务与 delta 规格。
- 新增确定性的同步脚本 `scripts/sync_wiki.py`，支持 `active` / `archive` / `all` 三种模式，
  校验 include 目标、delta 规格存在性与同步状态，并清理幽灵页面。
- 为 P01–P26 的全部归档资料和主规格执行初始同步，并把 WIKI 审计纳入最终交付门禁。

## F 阶段：后续修复与重构

前五个阶段是"按计划往前搭"，F 阶段是**跑起来之后回来改**。
这七个变更全部来自真实运行暴露的问题——写在纸上永远发现不了。

### 1. 修复诊断证据引用契约漂移

**做了什么**：把诊断证据种类收敛成**单一的运行时事实来源**（TypeScript 类型和 SSE 校验函数
引用同一份常量清单）；`reference.source` 在 AIOps 频道的校验覆盖全部种类，未知种类仍然拒绝；
补三处测试（契约层、后端合同层、前端 store 层）。

**为什么这么做**：用户一看到诊断证据就断流，提示"SSE 数据不符合共享事件合同"。
根因是**契约内部自相矛盾**：类型里定义了 7 种证据，但运行时校验把种类硬编码成了 4 种，
后端按完整定义发送，其中三类被自己的校验拒掉。

**留下什么**：证据种类只有一份清单，"两边各自维护"这个隐患被消除——这正是第 2 步"单一事实来源"原则的一次补课。

**我的思考**：这次错误的本质不是写错了某个类型，而是**同一个事实存在两份**。
只改数字不够，得让两边引用同一个来源，否则下次加证据类型还会再来一次。

### 2. 修复生成的查询转义与工具失败可见性

**做了什么**：中间产物 adapter 在提取模型生成的查询后**还原转义**，产出可直接执行的查询语句；
工具失败分类在脱敏前提下**保留一段服务端返回的消息**，让语法错误、字段错误在步骤、事件和审计里可见。

**为什么这么做**：模型生成的查询里带 JSON 转义（字面量 `\n`），被原样送进日志检索，
服务端把 `\nservice` 解析成字段名 `nservice` 直接报语法错误；而失败原因只保留了异常类型名，
真正的错误在系统里完全看不到，只能靠外部工具反推。

**留下什么**：生成的查询可用；失败原因可见。这也解释了之前"重跑就好了"的假象——
成功那次走的是服务端自拼的兜底查询（没有转义）。

**我的思考**：一个 bug 有两层——表层是转义没还原，深层是**失败原因被吞掉了**。
只修表层，下次换个原因还是查不出来，所以两层一起修。

### 3. 把完整工具说明书交给模型

**做了什么**：工具目录不再输出"只剩类型的摘要"，改为输出官方 MCP 返回的**完整 JSON Schema**
（字段说明、取值范围、格式、默认值、必填项）；唯一的裁剪是安全边界——
服务端注入的字段不进入模型可见的说明；同时删掉五条"替模型猜错买单"的兜底代码。

**为什么这么做**：模型填不对参数，根因不是能力不足，而是**它拿到的说明书被我们自己砍过**——
字段说明、取值范围、时间格式全丢了，稍复杂的类型还写成 `unknown`。
**完整 schema 本来就在手上，是我们裁掉的。**

**留下什么**：模型能看到和人类开发者一样的参数说明；兜底代码被删掉，问题暴露在源头而不是被掩盖。

**我的思考**：以前用一连串兜底（强制覆盖、秒级自动换算、别名映射）去接住模型猜错的参数，
但**兜底是在掩盖问题**——真正该做的是把该给的信息给全。

### 4. 重构诊断三节点分工与工具参数归属

**做了什么**：planner 不再输出参数，只描述"用哪些工具、按什么顺序、每步为了拿到什么"；
**executor 变成模型节点**，执行每一步前依据工具完整说明、整份计划、前序真实产出生成参数；
工具清单不再按静态白名单过滤（policy 从"准入名单"降级为"语义登记表"）；
入参校验通用化（此前对 MCP 工具**完全空转**）；重试改成每步最多两次并分三类；
executor 产出**结构化执行结果**（由代码拼装，含尝试次数、失败原因、真实产出）。

**为什么这么做**：第一版要求 planner 在**一次调用里写完全部内容，包括每一步的参数**，
但那一刻前面步骤的真实产出还不存在，模型只能填人话占位符。
**这不是提示词能修的，是职责划分错了。**

**留下什么**：三个节点的职责边界定清楚了；执行结果（结构化）成为后面报告和判据的输入；
这是一次 **BREAKING** 变更（计划里不再携带参数）。

**我的思考**：planner 只应该输出工具名字和步骤，告诉 executor 要执行什么；
真正填参数的是 executor——因为**只有执行到那一步，才知道前一步产出了什么**。
这也是我坚持的架构思路：让每个节点只做它掌握信息的那部分事。

### 5. 让诊断计划跑完并按报告信任状态判定结果

**做了什么**：executor 某一步失败后**推进进度指针继续跑完剩余步骤**（而不是直接中断）；
删掉 replanner 里"看到失败就提前出报告"的分支；报告提示词与校验器逐字对齐；
**任务终态由报告的信任状态决定**（可信 → 成功；证据不足 → 失败并带专用错误码；执行失败 → 失败）。

**为什么这么做**：跑起来之后发现两个问题——**一个工具失败会让后面所有步骤都不执行**，
于是报告必然证据不足；而且"报告生成了"被当成"任务成功"，
界面上明明写着"证据不足"却显示成功。

**留下什么**：计划能完整执行；任务状态与报告可信度一致。

**我的思考**：**一次工具调用失败不应该中止整个计划**，失败结果如实记录、继续往下走，
最后把判断交给验证层。另外"生成了一份报告"和"得出了可信结论"是两件事，界面必须分开表达。

### 6. 简化证据判据并退役拿不到参数的工具

**做了什么**：判据简化为三档，**"命中日志"成为唯一锚点**；
彻底删掉"时序上下文"这个概念（字段、校验规则、传参）；
把三个拿不到参数的工具标记为不可用并**退役**（说明原因，而不是静默失败）；
同时删掉报告节点一条"必须引用全部支撑证据否则降级"的隐藏门槛。

**为什么这么做**：原来的判据要求"必须有日志上下文证据"，但上传链路产不出那个工具需要的定位字段，
**等于永远判"证据不足"**——规则本身在真实数据上不可达。

**留下什么**：判据与真实可得的证据对齐；不可用的工具被明确退役而不是留成坑。

**我的思考**：判据不能建立在我们拿不到的证据上。那种情况下**要么想办法拿到，
要么把这条要求删掉**——留着只会让每一次诊断都"证据不足"。另外单条日志命中也是有效证据，
不能因为报警数量少就判失效。

### 7. 支持聊天模型使用独立端点与密钥

**做了什么**：配置里给聊天模型增加可选的独立端点和密钥（留空则回退到顶层）；
chat 的客户端构建、就绪探测、错误脱敏统一改用"生效值"；把厂商枚举放宽成标签；
缺上下文窗口登记时的报错改成可操作的提示。

**为什么这么做**：想把聊天模型换成另一家（embedding 和重排留在原厂），
但原来的配置只有一份端点和一把密钥，**表达不了"一份配置两家厂商"**。

**留下什么**：换模型只需要改配置，业务代码一行没动；顺带让脱敏能同时处理多把密钥。

**我的思考**：这是"边界先立好"的回报——第 6 步（模型统一入口）当初把三类能力的调用收在一处，
所以这次换厂商只是加两个可选字段，而不是全项目改一遍。

---

## 回头看：六个阶段

### 一、每一阶段都是同一个节奏

| 阶段 | 底座 | 数据 | 能力 | 界面 |
|---|---|---|---|---|
| A 地基 | 骨架 / 契约 / 仓储 / 认证 / 隔离 / 模型 / 设施 | — | — | — |
| B 知识库 | 持久任务 + 前端外壳 | 文档、切片、索引 | 混合检索 | 知识库页 |
| C 聊天 | （复用 B 的底座） | 会话、消息、记忆 | 流式 + 工具 + 审计 | 聊天工作台 |
| D AIOps | （复用 B 的底座） | 诊断任务、证据、报告、案例 | 诊断编排 + 证据门禁 | 诊断工作台 |

**底座 → 数据 → 能力 → 界面**，四个阶段都是这个顺序。
而且底座一次比一次薄：A 阶段打了七个变更的地基，B 和 C 各自只需要补一两块就够用了。

**界面永远排在最后**——因为界面是前面所有能力的入口，前面没定好，界面就要改。

### 二、三件反复出现的事（带具体做法）

**第一件：能在数据库里保证的，就不靠代码自觉。**

同一个套路我用过好几次，只是场景不同。挑三个最典型的：

**（1）同一个邮箱不许注册两次。**

做法就是给邮箱那一列加一个**唯一约束**——数据库层的一条规则：这一列不许出现两个一样的值。

那为什么不写成"注册之前先查一下这个邮箱在不在"？因为两个人**同时**注册同一个邮箱时，
两边都会查到"不存在"，然后两边都去插入，库里就多了两条一样的记录。
交给唯一约束之后，第二条会被数据库直接拒掉，并发再快也一样。

**（2）同一份文档的内容不许重复（这里用的是"部分唯一索引"）。**

"部分"的意思是：**这条不允许重复的规则，只对满足条件的那些行生效**。
我给它的条件是"还没被删除的行"（`deleted_at IS NULL`）。

为什么必须带上这个条件？因为覆盖上传的时候，旧文档不是真删掉，而是**软删除**——
数据行还在，只是打了一个"已删除"的时间标记。如果不带条件，新文档会因为
"被删掉的那条还占着位置"而永远插不进去。

带上条件之后效果正好：**旧版本留着可以追溯，活跃版本只允许有一份。**

**（3）同一个后台任务不能被两个 worker 抢走。**

做法是**把条件直接写进那条更新语句里**：只有"还在排队中"、或者"在运行但租约已经过期"的任务，
才允许被改成"运行中"，而且让这条语句把被改动的行返回给我。

为什么不能写成"先查一下有没有人抢，再决定要不要改"？因为查完到改之间有一个间隙，
两个 worker 可能都通过了检查。写成一条带条件的更新之后，数据库保证只有一个改得动，
另一个拿回来的是空的——也就是"没抢到"。

**一句话总结这条：把"检查"和"动作"合成同一次数据库操作，中间不留空隙。**

**第二件：同一件事只留一个入口。**

**（1）接口契约只有一个事实来源。**

这里的"契约"指的是前后端约定的那些东西，具体包括：

- **接口的路径和方法**：比如登录是 `POST /auth/login`；
- **请求体长什么样**：邮箱 + 密码，字段名和类型都固定；
- **成功响应的形状**：`{ ok: true, data: {...}, meta: {...} }`；
- **失败响应的形状**：`{ ok: false, error: {...} }`，其中错误码来自一份固定目录；
- **错误码目录**：比如"邮箱或密码错误"对应 401、"邮箱已注册"对应 409，每个码都写死状态码和默认文案；
- **流式事件类型**：`content.delta`、`tool.call`、`complete` 这些事件的名字和字段；
- **上传政策**：比如 Skill 文件必须叫 `SKILL.md`、不能超过 256 KB。

这些内容全都写在一份机器可读的清单里。**前端直接读这份清单，后端维护一份内容相同的镜像，
再加一条测试逐项比对两边。**

好处是加接口时不用"记得两边都改一遍"——只改清单，两边一起跟着变；忘记同步的话，测试立刻红。

**（2）切片只有一个函数。**

先说背景：一份长文档要**切成一块一块**才能算向量、才能被检索
而系统里有**两个地方**需要切片：

1. 上传之后给用户看的**切片预览**——让用户确认"切成了什么样"；
2. 真正入库时——把切好的块算成向量存进向量库。

如果这两处各写一套规则会怎样？比如预览按"每 1200 字一块"切，入库却按"按标题切"。
结果就是：**用户明明在预览里看到某句话在第 3 块，但怎么搜都搜不到**——
因为库里存的根本不是那一套切法切出来的块。这种问题两边单独看都正常，极难排查。

所以做法是：**两处都调用同一个函数**（`chunk_document_text`），
而且**这次用的切法、长度、重叠参数会跟着文档一起存进数据库**，
以后就算改了默认参数，也能说得清"当初是怎么切的"。
规格里还专门写了一条要求：**预览的结果必须能被索引用同样的方式复现**，并且有测试盯着。

**（3）隔离只有一个扫描脚本。**

它摆在测试里，跑一次就会遍历所有仓储类，检查每个公开方法的第一个参数是不是 owner。
所以"这次忘了加"会在门禁上直接失败，而不是靠人记得、或者靠代码评审看出来。

**一句话总结这条：每多留一个"第二入口"，就多一处将来会不一致的地方。**

**第三件：没做的事不假装。**

**（1）没配凭据时，就绪检查明确报"依赖不可用"并给出原因。**

而不是返回一个看起来正常的空结果。这样用户一眼就知道"是没配好"，
而不会误以为是"确实没有数据"。

**（2）证据不够，报告就写"证据不足"。**

不给一份看起来完整、其实没有根据的报告。宁可这次的结论是"没查出来"，
也不要编一份像模像样的东西糊过去。

**一句话总结这条：宁可说"不知道"或"没做到"，也不要给一个看起来正常的错答案。**

### 三、纸面设计是有边界的

前 28 个变更是"按计划往前搭"，最后 7 个全是**跑起来之后回来改**。

其中最有代表性的一次是：原本要求规划节点在一次调用里写完全部内容（包括每一步的工具参数），
但那一刻前面步骤的真实产出还不存在——**这是职责划分的问题，不是提示词能修的**。

这七个变更说明一件事：**真正的问题只有跑起来才会暴露**，
所以我把"先跑通一条最小链路、再回头改"当成方法，而不是把设计一次性想完美。

### 四、这套顺序带来的实际收益

- **换模型**只改了配置和三个文件，业务代码一行没动；
- **加一个新的长任务**（诊断、索引、案例沉淀
  重试、超时、取消、重启恢复全都是现成的；
- **加一个新的前端工作区**不用重新设计布局和状态管理，套用已有范式即可；
- **新增一个仓储**会被扫描脚本自动检查是否带了 owner 范围。

### 五、如果让我重做一次

我会更早做两件事：一是**更早拿真实数据跑通一条最小链路**（前 28 个变更做完才真跑，
导致后期集中返工）；二是**更早把"能看见 / 能调用 / 能验证 / 能作证"这四道门槛写清楚**，
因为工具治理里几乎所有问题都出在这四道门槛的混用上。

---

完整的变更记录在 [`openspec/changes/archive/`](./openspec/changes/archive)，
每个变更里都有提案、设计、任务和验收标准。
