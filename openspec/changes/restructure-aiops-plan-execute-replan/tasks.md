## 1. 前置与准备

- [x] 1.1 确认 `expose-full-aiops-tool-schemas` 已通过门禁并归档，工作区在其成果之上无残留未提交改动
- [x] 1.2 记录实施前的基线：后端全量 pytest 通过数量、`openspec validate --all` 结果、一次真实诊断的 step 与 evidence 记录（作为对照）

## 2. 工具集合来自真实发现（tool_policy）

- [x] 2.1 修改 `build_aiops_tool_registry`：不再按静态名单过滤，真实发现的只读工具全部进入 registry 与模型可见目录
- [x] 2.2 把 policy 条目重构为纯语义登记（能力、只读、依赖、产物类型、服务端注入字段），删除"未登记即排除"的行为
- [x] 2.3 确认模型可见目录始终提供完整参数 schema（名称 + 描述 + 全部字段说明），且服务端注入字段不出现在说明与必填列表中
- [x] 2.4 为只读判定补一条：真实发现中被标记为具有外部写副作用的工具必须被排除
- [x] 2.5 更新 `tests/aiops/test_tool_policy.py`：断言全部真实发现只读工具可见、未登记工具不被排除、服务端注入字段不可见

## 3. 计划只描述流程（planning）

- [ ] 3.1 从 `PlanStepDraft` 移除 `arguments`，为 `tool_name` / `purpose` 补齐 `Field(description=...)`
- [ ] 3.2 重写 planner 提示词为四段式（角色定义 / 任务 / 该做什么 / 输出结果），含"不填参数"的明确禁令与自查清单
- [ ] 3.3 修改 `validate_plan`：工具名匹配改为与语义登记一致的宽松匹配，拒绝时给出近似候选
- [ ] 3.4 删除 `query_is_trusted` 分支，改为"计划含日志检索则必须且只能有一个 query-builder 步骤且在检索之前"
- [ ] 3.5 为计划位置与数组下标的一致性加断言（或改为按 `position` 字段查找）
- [ ] 3.6 更新 `tests/aiops/test_contracts_and_planning.py`：计划不含参数、query-builder 强制、大小写宽松匹配、位置断言

## 4. 通用入参校验与产物兜底（tool_adapters）

- [x] 4.1 新增按真实 runtime schema 的通用入参校验：必填齐全、拒绝未声明键、类型匹配、范围与枚举合法（复用 `tool_schema_properties` / `tool_schema_required`）
- [x] 4.2 用通用校验替换对 MCP 工具空转的 `validate_runtime_tool_input`，保留核心 CLS 工具的显式 Pydantic 输入模型与规范化
- [x] 4.3 为未登记输出 adapter 的只读工具补统一中间产物兜底：产物按中间产物落库，不进入证据充分性判断
- [x] 4.4 补测试：缺必填、含未声明键、类型错误、越界取值被本地拒绝且不产生真实工具调用

## 5. 错误分类与重试策略（tool_failures）

- [x] 5.1 把失败分类拆成"原样重试 / 修正后重试 / 不可重试"三类，并把分类结果暴露给执行结果
- [x] 5.2 修正外部执行错误形式的入参校验失败（`-32602` 一类）被误判为可重试传输错误的问题
- [x] 5.3 保持"不可重试"错误立即停止该步重试，不得交给模型猜测修复
- [x] 5.4 更新 `tests/aiops/test_tool_recovery.py`：入参错误进入修正流程、不可重试错误只产生一次 attempt、分类结果可读

## 6. 执行节点重写（runtime executor）

- [ ] 6.1 重写 executor 为按步填参：每一步执行前调用模型生成该步参数
- [ ] 6.2 填参输入限定为"当前工具的完整参数说明 + 整份计划 + 前面步骤的真实完整产出 + 告警原文"，不传全部工具 schema；前序产出不得摘要截断
- [ ] 6.3 为填参新增模型方法与四段式提示词，输出带字段描述的参数模型
- [ ] 6.4 参数装配顺序固定为：模型填参 →（服务端注入 Region/TopicId）→ 通用入参校验 → 调用；模型填写值不得覆盖服务端注入字段
- [ ] 6.5 把每个步骤的尝试上限改为两次，计数按计划步骤位置
- [ ] 6.6 executor 不得选择工具、调整顺序或追加步骤；越界需求一律交回 replanner
- [ ] 6.7 更新 `tests/aiops/test_runtime.py`：按步填参、服务端字段不被覆盖、两次上限、不可重试立即停止、执行者不越界

## 7. 执行结果与 replanner（runtime）

- [ ] 7.1 用真实产出的安全摘要替换写死的 `result_summary="真实工具调用成功"`
- [ ] 7.2 新增执行结果组装：每步的工具名、真实参数、尝试次数、每次失败原因与类别、成功产出、按类型分组的证据及来源步骤
- [ ] 7.3 执行结果做脱敏：不得出现凭据、Region、TopicId 或其他敏感参数值
- [ ] 7.4 replanner 改为纯代码验证：依据证据充分性、剩余步骤与预算决定继续或出报告，不调用模型、不改写计划
- [ ] 7.5 保留 `ReplanDraft` / `replan()` / `MAX_REPLANS` 代码路径但不触发，确认无死代码告警
- [ ] 7.6 补测试：执行结果内容完整且不含敏感值、replanner 不调模型、达到上限后路由正确

## 8. 报告节点接入执行结果

- [ ] 8.1 报告生成器的输入加入执行结果，使报告能说明未完成步骤及其失败类别
- [ ] 8.2 补测试：存在未执行或失败步骤时报告如实说明，不虚构执行成功

## 9. 门禁与真实验证

- [ ] 9.1 在 `apps/backend` 运行 `uv run ruff check .`，通过
- [ ] 9.2 在 `apps/backend` 运行 `uv run pyright`，零错误
- [ ] 9.3 在 `apps/backend` 运行 `uv run pytest` 全量，通过
- [ ] 9.4 运行 `openspec validate --all`，通过
- [ ] 9.5 运行 `git diff --check`，通过
- [ ] 9.6 真实 smoke：上传 CLS 日志、发布告警后跑一次完整诊断，核对计划不含参数、参数来自前序真实产出、失败分类正确、执行结果完整、任务状态与真实结果一致
- [ ] 9.7 记录 smoke 结论到笔记：解决了什么、仍然存在什么（含"类型合法但值是废话"仍未覆盖）

## 10. 归档与文档

- [ ] 10.1 用 `openspec-verify-change` 检查完整性、正确性与设计一致性，修复全部 CRITICAL 问题
- [ ] 10.2 同步 delta specs 到主规格并按 Conventional Commits 提交
- [ ] 10.3 运行 `wiki-sync` 同步 `docs/changes/`，并确认 `npm run docs:build` 通过
- [ ] 10.4 归档变更并更新 Obsidian 笔记：进度总览、当前任务状态、已知问题与待改进
