## 1. 准备与基线

- [x] 1.1 记录实施前基线：后端全量 pytest 通过数、`openspec validate --all` 结果，以及任务 `ad4a4bd747854aa4add3d9650f411682` 的步骤/证据/报告记录作为对照
- [x] 1.2 记录离线复现结论：模型报告正文与校验器标签要求的差异（用于回归测试的断言依据）

## 2. Executor 失败后继续推进计划

- [x] 2.1 修改 executor：某一步尝试耗尽时，在记录该步失败后把进度指针推进到下一步
- [x] 2.2 确认推进后的 off-by-one 语义：进度指针始终指向"下一个待处理位置"，成功的步骤与失败的步骤语义一致
- [x] 2.3 确认 worker 恢复路径：断点定位仍以"已处理的步骤"为准，不把失败步骤当成未处理而重跑
- [x] 2.4 补测试：某一步失败后其余步骤仍被执行，直至计划全部处理完毕

## 3. Replanner 路由只看进度与证据

- [x] 3.1 删除 Replanner 中"存在失败标记即转报告"的提前退出条件
- [x] 3.2 保留 `last_error` 作为"本轮有步骤失败过"的说明信息，供报告节点使用，不再参与路由
- [x] 3.3 确认循环有界：指针持续推进 + 计划步数上限 8，不再依赖失败标记防死循环
- [x] 3.4 补测试：执行结果中存在失败步骤但仍有剩余步骤时，控制权交回 Executor；计划全部处理后才转报告

## 4. 执行结果结构完整

- [x] 4.1 确认执行结果为计划的每一步产出记录（含未执行、失败、无产出的标注）
- [x] 4.2 补测试：全部步骤失败且无任何证据时，执行结果仍覆盖计划的每一步
- [x] 4.3 补测试：执行结果仍不含凭据、Region、TopicId 等敏感取值

## 5. 报告提示词与校验器逐字对齐

- [x] 5.1 重写报告提示词：以字面量给出 `详情：` `症状：` `日志证据：` `根因结论：` `已执行步骤：` `建议：` `预期效果：` `整体评估：` `关键发现：` `后续建议：` `风险评估：`（含全角冒号）
- [x] 5.2 在提示词中给出完整结构示例（含逐告警编号段），使模型可以照抄结构
- [x] 5.3 确认提示词列出的标签集合与 `reporting.py` 的校验常量一一对应，避免再次漂移
- [x] 5.4 补测试：断言报告提示词包含校验器要求的全部字面标签

## 6. 任务终态由报告信任状态决定

- [x] 6.1 在共享错误目录新增"证据不足、无法给出可信结论"的稳定错误码（唯一 code、system category、安全默认消息）
- [x] 6.2 同步 `packages/api-contracts` 的错误码枚举与合同测试
- [x] 6.3 修改 report 节点：`verified_evidence` → `succeeded`；`insufficient_evidence` → `failed` + 新错误码；`execution_failed` → `failed`（现有语义）
- [x] 6.4 确认兜底报告与已收集证据链仍然持久化并可读，失败的是结论可信度而不是可观测性
- [x] 6.5 补测试：三种信任状态分别落到正确的任务终态与 `failureCode`
- [x] 6.6 补回归测试：证据不足的任务不再进入案例沉淀链路

## 7. 前端按终态展示

- [x] 7.1 前端 AIOps 工作区按任务终态与 `failureCode` 区分"诊断成功"与"证据不足未能得出结论"
- [x] 7.2 补前端测试：证据不足的诊断不显示为成功

## 8. 门禁与真实验证

- [x] 8.1 在 `apps/backend` 运行 `uv run ruff check .`，通过
- [x] 8.2 在 `apps/backend` 运行 `uv run pyright`，零错误
- [x] 8.3 在 `apps/backend` 运行 `uv run pytest` 全量，通过
- [x] 8.4 运行 `npm run contracts:typecheck` 与契约包测试，通过
- [x] 8.5 运行 `npm run frontend:typecheck`、`npm run frontend:test`、`npm run frontend:build`，通过
- [x] 8.6 运行 `openspec validate --all` 与 `git diff --check`，通过
- [x] 8.7 真实 smoke：先传日志、等入库、再发告警，跑一次完整诊断，核对"某步失败后其余步骤仍执行""报告不再因标签写法回退""任务终态与信任状态一致"
- [x] 8.8 把 smoke 结论记入笔记（解决了什么、仍存在什么）

## 9. 归档与文档

- [x] 9.1 用 `openspec-verify-change` 检查完整性、正确性与设计一致性，修复全部 CRITICAL 问题
- [x] 9.2 同步 delta specs 到主规格并按 Conventional Commits 提交
- [x] 9.3 运行 `wiki-sync` 同步 `docs/changes/`，并确认 `npm run docs:build` 通过
- [x] 9.4 归档变更并更新 Obsidian 笔记：进度总览、当前任务状态、已知问题与待改进
